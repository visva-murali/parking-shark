node_modules/
.env
.env.local
npm-debug.log*
.DS_Store
*.log
coverage/

# Claude Code settings
.claude/

# Playwright browser test artifacts
.playwright-mcp/

# Test screenshots generated at repo root
/*.png

# GCP deployment config (contains production secrets — use deploy/app.yaml as template)
deploy/
app.yaml

# Live database dump — use sql/ schema files instead
parking_shark.sql
